import { Dictionary } from '@/i18n/types'
import { NextPage } from 'next'

interface Props {
  dict: Dictionary
}

const Projects: NextPage<Props> = ({ dict }) => {
  return <div></div>
}

export default Projects